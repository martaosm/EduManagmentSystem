#timetable-service-service-monitoring.yaml
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: timetable-service
  labels:
    team: backend
  namespace: monitoring
spec:
  selector:
    matchLabels:
      app: timetable-service
  endpoints:
    - port: 8080