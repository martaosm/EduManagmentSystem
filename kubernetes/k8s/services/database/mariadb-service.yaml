#mariadb-clusterip-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: mariadb-service
  namespace: database
spec:
  selector:
    app: mariadb
  ports:
    - protocol: TCP
      port: 3306
      nodePort: 31338
      targetPort: 3306
  type: LoadBalancer
