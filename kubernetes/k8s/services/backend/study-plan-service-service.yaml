#study-plan-service-service-monitoring.yaml
apiVersion: v1
kind: Service
metadata:
  name: study-plan-service
  namespace: backend
spec:
  selector:
    app: study-plan-service
  ports:
    - port: 8080
      targetPort: 8080
  type: LoadBalancer
