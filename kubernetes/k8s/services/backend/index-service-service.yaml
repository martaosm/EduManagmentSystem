#index-service-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: index-service
  namespace: backend
spec:
  selector:
    app: index-service
  ports:
    - port: 8080
      targetPort: 8080
  type: LoadBalancer
