export interface ClassesQueryModel {
  groupCode: string,
  courseName: string,
  classType: string,
  courseCode: string,
  classDate: string,
  registeredStudents: number,
} //TODO WB: Check if all needed fields are here
